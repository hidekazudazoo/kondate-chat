<% if current_user.favorites.find_by(recipe_id: recipe.id) %>
  <%= link_to 'お気に入りをはずす', recipe_favorite_path(recipe.id), method: :delete, remote: true %>
<% else %>
  <%= link_to 'お気に入り登録', recipe_favorites_path(recipe.id), method: :post, remote: true %>
<% end %>