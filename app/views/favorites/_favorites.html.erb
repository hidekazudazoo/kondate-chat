<% if current_user.favorites.find_by(recipe_id: recipe.id) %>
  <%= link_to '登録', recipe_favorite_path(recipe.id), method: :delete, remote: true %>
<% else %>
  <%= link_to '解除', recipe_favorites_path(recipe.id), method: :post, remote: true %>
<% end %>