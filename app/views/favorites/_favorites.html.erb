<% if current_user.favorites.find_by(recipe_id: recipe.id) %>
  <%= link_to 'in', recipe_favorite_path(recipe.id), method: :delete, remote: true %>
<% else %>
  <%= link_to 'out', recipe_favorites_path(recipe.id), method: :post, remote: true, class:"favorite-btn" %>
<% end %>